const {
  connectAgent,
  subscribeToConversations,
  generateURLForDownloadFile,
  sendMessage,
} = require('./functions');

module.exports = {
  connectAgent,
  subscribeToConversations,
  generateURLForDownloadFile,
  sendMessage,
};
